// Placeholder — the real finale (Doubt Prompt, endings A/B) lands in Task 18.
// This stub exists only so `import('./engine/finale')` in main.ts resolves
// and `tsc --noEmit` stays clean before Task 18 replaces it.
export class Finale {
  constructor(_deps: any) {}
  async start() {
    console.warn('finale lands in Task 18')
  }
}
